[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "oml-tools"
version = "0.1.0"
description = "Validator, exporter and site builder for the Open Misconception Library"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = ["jsonschema>=4.18"]

[project.scripts]
oml = "oml.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["oml*"]
